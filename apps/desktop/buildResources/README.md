# Build Resources

Place platform-specific application icons here before release packaging:

- `icon.icns` for macOS
- `icon.ico` for Windows
- `icon.png` as a general fallback

`electron-builder` can still package with Electron defaults, but shipping custom icons is recommended before the first public build.
